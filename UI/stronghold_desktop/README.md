# stronghold_desktop

Windows desktop aplikacija za administraciju Stronghold teretane.

Pokretanje (backend mora biti podignut, upute u glavnom README-u):

```
flutter run -d windows --dart-define=API_BASE_URL=http://localhost:5000
```
